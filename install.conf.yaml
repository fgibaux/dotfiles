- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.gitconfig:
    ~/.screenrc:
    ~/.antigen:
    ~/.zshrc:
    ~/.tmux.conf:
    ~/.tmux:
    ~/.vim:
    ~/.vimrc:
    

- shell:
    - [git submodule update --init --recursive, Installing/updating submodules]
